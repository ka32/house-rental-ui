import { PostStatusTypePipe } from './post-status-type.pipe';

describe('PostStatusTypePipe', () => {
  it('create an instance', () => {
    const pipe = new PostStatusTypePipe();
    expect(pipe).toBeTruthy();
  });
});
